name: cloudflare_calls
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
environment:
  sdk: ^3.2.6

# Add regular dependencies here.
dependencies:
  build_runner: ^2.4.9
  openapi_generator_annotations: ^5.0.2

dev_dependencies:
  lints: ^2.1.0
  openapi_generator: ^5.0.2
  test: ^1.24.0
